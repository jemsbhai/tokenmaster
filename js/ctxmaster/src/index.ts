/**
 * ctxmaster: visualization layer for tokenmaster.
 *
 * JavaScript port scaffold; the terminal context gauge lands next. This
 * entry point currently proves the dependency wiring: the core resolves,
 * links, and type-checks through both build formats.
 */

import { SCHEMA_VERSION } from "tokenmaster";

export const CORE_SCHEMA = SCHEMA_VERSION;
